import { QueryParams } from "../../common/api/query";

export type OperatorQuery = QueryParams<"">;

// export function getCollection(params: StandardParams<HubQuery>) {
//   return Request.getRequest<Collection>({
//     ...params,
//     target: links.hubs().collection(),
//   });
// }
